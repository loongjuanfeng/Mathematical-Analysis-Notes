#let vc(x) = [$bold(upright(harpoon(#x)))$] // Vector
#let mt(A) = [$bold(upright(#A))$]  // Matrix
#let diag = [$op("diag")$]  // diag
#let rank = [$op("rank")$]  // rank
#let ip(x, y) = [$lr(chevron.l #x, #y chevron.r)$]  // inner product
