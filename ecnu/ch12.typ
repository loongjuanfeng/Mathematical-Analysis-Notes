#import "../../libs.typ": *
#import "../syms.typ": *

#show: ilm.with(
  title: [Mathematical Analysis],
  date: datetime.today(),
  author: "Junfeng Lve",
  abstract: [Study notes on Chapter 12 of _数学分析_，陈纪修.],
)
#show: setup

// #todo[补全][
//   = 多元函数的微分
//   == 偏导数、方向导数
//   == 全微分、梯度
//   == 高阶微分
//
//   = 多元复合函数的求导法则
//
//   = 一阶全微分的形式不变性
//
//   = 中值定理
//
//   = Taylor公式
//
//   = 隐函数
//
//   = 偏导数和几何
//
//   = 无条件极值
//
//   = 条件极值
// ]

= 多元函数的微分

#motivation[从连续到可微][]
连续性只说明当自变量靠近时函数值也会靠近，但它还没有告诉我们“靠近得有多线性”。可微性则进一步要求：在足够小的尺度上，多元函数的增量能被一个线性函数主导。这正是偏导数、方向导数、全微分与梯度要刻画的核心。

#overview[本章的理解路线][
  + 先看函数沿单个方向怎样变化，得到偏导数与方向导数。
  + 再追问这些方向变化能否被同一个线性对象统一，得到全微分与梯度。
  + 接着研究变量嵌套和局部展开，得到链式法则、中值定理与Taylor公式。
  + 最后把这些工具落到隐函数、几何解释和极值问题上。
]

按人的理解顺序，最自然的问题通常不是一上来写一般公式，而是：先沿某个方向动一点会怎样；再问所有方向的变化能否被统一描述；再问变量之间若有依赖关系，求导法则会怎样改变；最后再问这些局部信息究竟能帮我们判断什么。下面就按这条线索展开。

== 偏导数、方向导数

最先能想到的做法，是先把别的变量按住不动，只让一个坐标单独变化。这样得到的是最朴素、也最容易计算的变化率。

#definition[偏导数][
  设 $f$ 定义在 $E subset.eq RR^m$ 上，$vc(a) = (a_1, a_2, dots, a_m)$ 是 $E$ 的内点。若极限
  $
    lim_(t -> 0) (f(a_1, dots, a_i + t, dots, a_m) - f(vc(a))) / t
  $
  存在，则称它为 $f$ 在 $vc(a)$ 处关于第 $i$ 个变量的偏导数，记作
  $
    f_(x_i)(vc(a))
  $
]

但坐标轴方向只是所有可能方向里的极少数。若想真正理解函数在一点附近的局部变化，就不能只沿着“横着走”或“竖着走”来看，还要允许沿任意方向切入。

#definition[方向导数][
  设 $vc(l) = (l_1, l_2, dots, l_m)$ 是单位向量，即 $norm(vc(l)) = 1$。若极限
  $
    D_(vc(l)) f(vc(a)) = lim_(t -> 0) (f(vc(a) + t vc(l)) - f(vc(a))) / t
  $
  存在，则称它为 $f$ 在 $vc(a)$ 处沿方向 $vc(l)$ 的方向导数。
]

#remark[坐标方向只是特殊方向][]
取标准基向量 $vc(e)_i$ 时，方向导数就退化为对应偏导数：
$
  D_(vc(e)_i) f(vc(a)) = f_(x_i)(vc(a))
$
所以偏导数只是沿坐标轴方向观察到的变化率，而方向导数允许沿任意给定方向切入。

#intuition[把方向导数看成“试探坡度”][]
可以把点 $vc(a)$ 想成山坡上的一个落脚点。偏导数是只许你沿东西、南北这些坐标方向迈步时测到的坡度；方向导数则允许你朝任意方向迈出极小一步，看看海拔瞬间怎么变。多元微分真正想回答的是：这些不同方向上的坡度，能不能被同一个线性规律统一描述。

#example[偏导存在不推出可微][
  令
  $
    f(x, y) = x y / (x^2 + y^2)^(1 / 2) quad ((x, y) != (0, 0)), quad f(0, 0) = 0
  $
  则 $f_x(0, 0) = f_y(0, 0) = 0$。但当 $y = x$ 时，
  $
    f(x, x) = abs(x) / 2^(1 / 2)
  $
  因而
  $
    f(x, x) / norm((x, x)) = 1 / 2
  $
  不趋于 $0$，所以 $f$ 在原点不可微。
]

#connection[为什么还要全微分][]
这个例子说明：把若干方向上的斜率分开记住，还不足以保证函数在该点附近真的像某个平面。于是下一个自然问题就是：能否找到一个统一的线性近似，同时控制所有足够小的位移？

== 全微分、梯度

这正引出“可微”这个更强的概念。它不再分别看某几个方向，而是要求一切足够小的位移都服从同一个线性主部。

#definition[可微与全微分][
  设 $Delta vc(x) = (Delta x_1, dots, Delta x_m)$，并记
  $
    Delta u = f(vc(a) + Delta vc(x)) - f(vc(a))
  $
  若存在关于 $Delta vc(x)$ 的线性函数
  $
    L(Delta vc(x)) = sum_(i = 1)^m A_i Delta x_i
  $
  使得
  $
    Delta u = L(Delta vc(x)) + o(norm(Delta vc(x))) quad (norm(Delta vc(x)) -> 0)
  $
  则称 $f$ 在 $vc(a)$ 处可微。线性主部
  $
    dif u = L(Delta vc(x))
  $
  称为 $f$ 在 $vc(a)$ 处的一阶全微分。
]

#intuition[可微就是局部看起来像平面][]
一元可导意味着曲线在一点附近像它的切线；多元可微意味着曲面在一点附近像它的切平面。式子里的 $o(norm(Delta vc(x)))$ 表示：真实曲面和这个线性近似之间的误差，比位移本身还要小一个量级，所以在足够近的尺度上，线性主部才是支配项。

#theorem[可微时偏导数决定线性主部][
  若 $f$ 在 $vc(a)$ 处可微，则各偏导数 $f_(x_i)(vc(a))$ 都存在，且
  $
    dif u = sum_(i = 1)^m f_(x_i)(vc(a)) dif x_i
  $
]

#definition[梯度][
  若 $f$ 在 $vc(a)$ 处各偏导数存在，则称向量
  $
    nabla f(vc(a)) = (f_(x_1)(vc(a)), f_(x_2)(vc(a)), dots, f_(x_m)(vc(a)))
  $
  为 $f$ 在 $vc(a)$ 处的梯度。
]

#theorem[方向导数的梯度表达式][
  若 $f$ 在 $vc(a)$ 处可微，$vc(l)$ 为单位向量，则
  $
    D_(vc(l)) f(vc(a)) = sum_(i = 1)^m f_(x_i)(vc(a)) l_i = ip(nabla f(vc(a)), vc(l))
  $
]

#intuition[梯度是把所有局部斜率打包成一个箭头][]
与其把每个偏导数零散地记住，不如用一个向量把它们打包。这个向量和任意单位方向做内积，就给出那个方向上的方向导数；因此梯度既是“局部斜率信息的压缩包”，又天然带着几何方向感。

#theorem[连续偏导是可微的充分条件][
  若 $f$ 在 $vc(a)$ 的某邻域内各偏导数都存在，且这些偏导数在 $vc(a)$ 连续，则 $f$ 在 $vc(a)$ 处可微。
]

#remark[可微比偏导存在更强][]
偏导数只是在坐标轴方向上分别考察变化率，可微则要求所有方向的联合变化都被同一个线性函数统一近似。因此“偏导都存在”并不足以推出可微，“偏导连续”才给出一个常用而实用的充分条件。

#example[线性近似][
  对
  $
    f(x, y) = (1 + x + y)^(1 / 2)
  $
  在点 $(0, 0)$ 附近，有
  $
    f_x(0, 0) = f_y(0, 0) = 1 / 2
  $
  因而
  $
    f(x, y) = 1 + (x + y) / 2 + o(norm((x, y)))
  $
  这说明在足够小的尺度上，$f$ 可以用切平面
  $
    z = 1 + (x + y) / 2
  $
  近似。
]

== 高阶微分

一阶全微分给出的是“切平面”这一层信息；若还想继续区分曲面是向上弯、向下弯，还是像马鞍那样一边上升一边下降，就需要进一步追踪二阶变化。

#definition[二阶偏导与混合偏导][
  若偏导数 $f_(x_i)$ 在 $vc(a)$ 附近仍可求偏导，则
  $
    f_(x_i x_j)(vc(a))
  $
  称为 $f$ 在 $vc(a)$ 处的二阶偏导数。当 $i != j$ 时，它也称为混合偏导数。
]

#theorem[混合偏导可交换][
  若二阶偏导数 $f_(x_i x_j)$ 与 $f_(x_j x_i)$ 在 $vc(a)$ 的某邻域内存在，并且都在 $vc(a)$ 连续，则
  $
    f_(x_i x_j)(vc(a)) = f_(x_j x_i)(vc(a))
  $
]

#definition[二阶全微分与Hessian矩阵][
  若 $f$ 具有连续二阶偏导数，则它在 $vc(a)$ 处的二阶全微分定义为
  $
    dif^2 u = sum_(i = 1)^m sum_(j = 1)^m f_(x_i x_j)(vc(a)) dif x_i dif x_j
  $
  由二阶偏导数
  $
    f_(x_i x_j)(vc(a)) quad (i, j = 1, 2, dots, m)
  $
  排成的矩阵记为
  $
    mt(H)_f(vc(a))
  $
  称为 $f$ 在 $vc(a)$ 处的Hessian矩阵。
]

#remark[二元情形的展开][]
当 $m = 2$ 且混合偏导可交换时，
$
  dif^2 u = f_(x x)(dif x)^2 + 2 f_(x y) dif x dif y + f_(y y)(dif y)^2
$
它已经是一个二次型，后面判别极值时正是靠这个二次型的符号。

#example[二次函数的高阶微分][
  若
  $
    f(x, y) = x^2 + x y + y^2
  $
  则
  $
    dif f = (2 x + y) dif x + (x + 2 y) dif y
  $
  $
    dif^2 f = 2 (dif x)^2 + 2 dif x dif y + 2 (dif y)^2
  $
  这里二阶全微分与点无关，因为二次函数的二阶偏导数本来就是常数。
]

#connection[从独立变量到复合变量][]
到这里，我们已经会描述函数在一点附近的一阶和二阶局部结构。但很多实际变量并不是彼此独立给出的，而是由别的参数进一步决定，于是自然要进入复合函数的求导法则。

= 多元复合函数的求导法则

#motivation[复合结构是多元微分的常态][]
上一节讨论的是“变量彼此独立”时的局部变化。可在实际问题中，坐标会换元，状态会依赖参数，最终函数往往是“函数套函数”。链式法则的作用，就是把最外层对中间变量的变化率与中间变量对底层变量的变化率依次串起来。

理解链式法则时，最好先从“所有变量都依赖于同一个参数”的情形入手。这个版本最接近一元函数求导，也最容易看出“变化率是怎样一层层传递的”。

#theorem[一元参数的链式法则][
  设
  $
    u = f(x_1, x_2, dots, x_m), quad x_i = x_i(t)
  $
  若 $f$ 在相应点可微，而各 $x_i(t)$ 可导，则复合函数 $u(t)$ 可导，且
  $
    (dif u) / (dif t) = sum_(i = 1)^m f_(x_i) frac(dif x_i, dif t)
  $
]

#theorem[一般链式法则][
  设
  $
    z = f(y_1, y_2, dots, y_n), quad y_j = y_j(x_1, x_2, dots, x_m)
  $
  若这些函数在相应点可微，则对每个 $i = 1, 2, dots, m$ 都有
  $
    frac(partial z, partial x_i) = sum_(j = 1)^n frac(partial f, partial y_j) frac(partial y_j, partial x_i)
  $
]

#remark[链式法则的本质][]
它说的不是“把式子机械地一层层求导”，而是：总变化率等于各条传播路径上局部变化率的加总。这一点在坐标变换、隐函数求导和优化问题中会反复出现。

#intuition[变化率像水流一样层层传递][]
把最底层变量的一个微小扰动想成一滴水，它会先流到中间变量，再传到最外层函数。链式法则做的事，就是把每一段通道上的放大或缩小倍率乘起来，再把所有可能路径的贡献加总，所以复合结构越复杂，链式法则越像一张“影响传播图”。

#example[极坐标变换下的偏导][
  设
  $
    z = f(x, y), quad x = r cos theta, quad y = r sin theta
  $
  则
  $
    frac(partial z, partial r) = f_x cos theta + f_y sin theta
  $
  $
    frac(partial z, partial theta) = - r f_x sin theta + r f_y cos theta
  $
  这些公式说明：换元后的偏导数，本质上是原来梯度在新坐标方向上的投影。
]

#connection[链式法则用微分语言重写][]
链式法则一旦改用微分记号来表达，公式的结构会变得异常整齐。这也正是一阶全微分“形式不变性”的来源。

= 一阶全微分的形式不变性

#motivation[变量换了，微分形式不变][]
链式法则展开以后，会发现一阶全微分始终保持
$
  dif u = sum_(i = 1)^m f_(x_i) dif x_i
$
的外形。所谓“形式不变”，不是说数值没有变化，而是说无论把 $x_i$ 看成自变量还是中间变量，微分的线性表达式都沿用同一套写法。

#theorem[一阶全微分的形式不变性][
  若
  $
    u = f(x_1, x_2, dots, x_m), quad x_i = x_i(t_1, t_2, dots, t_n)
  $
  且各函数在相应点可微，则
  $
    dif u = sum_(i = 1)^m f_(x_i) dif x_i
  $
  其中每个
  $
    dif x_i = sum_(j = 1)^n frac(partial x_i, partial t_j) dif t_j
  $
  把它代回上式，便得到关于 $t_1, t_2, dots, t_n$ 的链式法则。
]

#remark[“形式不变”只对一阶全微分成立][]
二阶及更高阶微分在变量替换下会出现额外项，所以不能像一阶全微分那样简单地把中间变量整体替换掉。这也是一阶微分在几何与物理中格外自然的原因。

#intuition[一阶微分像一本记账簿][]
一阶全微分只记录“每个自变量各动一点，会给函数值带来多少一阶变化”。至于这些自变量本身又怎样依赖别的参数，只需把对应的 $dif x_i$ 再展开记账即可，所以它在变量替换下还能保持同样外形。

#example[极坐标下的微分][
  对
  $
    z = f(x, y), quad x = r cos theta, quad y = r sin theta
  $
  有
  $
    dif z = f_x dif x + f_y dif y
  $
  而
  $
    dif x = cos theta dif r - r sin theta dif theta
  $
  $
    dif y = sin theta dif r + r cos theta dif theta
  $
  代入后得到
  $
    dif z = (f_x cos theta + f_y sin theta) dif r + (- r f_x sin theta + r f_y cos theta) dif theta
  $
]

#connection[从局部微分到两点增量][]
一阶全微分给出的是点附近最基本的线性控制。接下来一个自然问题是：若比较两个不同点的函数值差，这种局部一阶信息能否在整条线段上积累起来？这就进入中值定理。

= 中值定理

#motivation[把多元问题压回一元][]
有了局部线性模型后，我们自然会问：它能对两点之间的函数差值提供什么控制？多元中值定理的技巧是：不直接在整个空间里做文章，而是把两点之间的线段参数化成一个一元函数，然后调用一元Lagrange中值定理。这样就把“多元增量”变成了“沿线段的单变量变化”。

#theorem[有限增量公式][
  设 $D subset.eq RR^m$ 为凸开集，$f : D -> RR$ 在 $D$ 上可微。则对任意 $vc(a), vc(b) in D$，存在 $theta in (0, 1)$，使
  $
    f(vc(b)) - f(vc(a)) = sum_(i = 1)^m f_(x_i)(vc(a) + theta (vc(b) - vc(a))) (b_i - a_i)
  $
  也即
  $
    f(vc(b)) - f(vc(a)) = ip(nabla f(vc(a) + theta (vc(b) - vc(a))), vc(b) - vc(a))
  $
]

#intuition[多元中值定理其实只看一条线段][]
虽然函数定义在整个空间里，但比较两点时，真正相关的只是它们之间那条线段。沿着这条线段走，原来的多元函数就退化成一个一元函数，于是“某处瞬时变化率乘以总位移”这一一元图像会原封不动地回来。

#corollary[梯度有界时的估计][
  若在凸开集 $D$ 上有
  $
    norm(nabla f(vc(x))) <= M
  $
  则对任意 $vc(a), vc(b) in D$ 都有
  $
    abs(f(vc(b)) - f(vc(a))) <= M norm(vc(b) - vc(a))
  $
]

#corollary[梯度为零则函数为常数][
  若 $D$ 为凸开集，$f$ 在 $D$ 上可微，且对一切 $vc(x) in D$ 都有
  $
    nabla f(vc(x)) = 0
  $
  则 $f$ 在 $D$ 上为常数。
]

#remark[它和一元中值定理的关系][]
当 $m = 1$ 时，上式就退化成熟悉的
$
  f(b) - f(a) = f'(a + theta (b - a)) (b - a)
$
所以多元中值定理并不是全新的结构，而是一元中值定理沿线段的自然推广。

#connection[从一阶控制到二阶展开][]
中值定理把两点间的差值压缩成了某处的一阶信息。若我们不只想知道“差值大概由一阶导控制”，而想更细致地拆出一次项、二次项乃至更高阶项，就会走到Taylor公式。

= Taylor公式

#motivation[用多项式逼近局部结构][]
中值定理说明一阶导数能够控制增量，Taylor公式则更进一步：它把函数在一点附近的行为按阶次逐层拆开。对多元函数来说，二次项尤其重要，因为它直接决定驻点附近是谷底、山顶还是鞍点。

若只从后面极值判别的应用出发，二阶公式已经够用；但从理论上说，Taylor公式并不止于二阶，而是有一套完整的 $n$ 阶展开。先把最一般的形式写出来，再把二阶情形单独拎出来看，会更清楚它们之间的关系。

#notation[多重指标记号][
  设
  $
    alpha = (alpha_1, alpha_2, dots, alpha_m)
  $
  是一组非负整数，记
  $
    abs(alpha) = sum_(i = 1)^m alpha_i, quad alpha! = product_(i = 1)^m alpha_i ! \
    vc(h)^alpha = product_(i = 1)^m h_i^alpha_i
  $
  $
    D^alpha f = frac(partial^(abs(alpha)) f, partial x_1^(alpha_1) partial x_2^(alpha_2) dots partial x_m^(alpha_m))
  $
]

#theorem[$n$ 阶Taylor公式][
  设 $f$ 在 $vc(a)$ 的某邻域内具有直到 $n$ 阶的连续偏导数，记 $vc(h) = (h_1, h_2, dots, h_m)$。则当 $vc(h) -> 0$ 时，
  $
    f(vc(a) + vc(h)) = sum_(abs(alpha) <= n) (D^alpha f(vc(a))) / (alpha!) vc(h)^alpha + o(norm(vc(h))^n)
  $
]

#theorem[带Lagrange余项的 $n$ 阶公式][
  在同样条件下，存在 $theta in (0, 1)$，使
  $
    f(vc(a) + vc(h)) = sum_(abs(alpha) <= n - 1) (D^alpha f(vc(a))) / (alpha!) vc(h)^alpha + sum_(abs(alpha) = n) (D^alpha f(vc(a) + theta vc(h))) / (alpha!) vc(h)^alpha
  $
]

#remark[二阶公式只是最常用的特例][]
把上式取 $n = 2$，就得到后面最常用的二阶Taylor公式。分析里之所以总是反复强调二阶，不是因为一般公式不重要，而是因为二阶项恰好已经足够刻画局部弯曲和极值性质。

#theorem[二阶Taylor公式][
  设 $f$ 在 $vc(a)$ 的某邻域内具有连续二阶偏导数，记 $vc(h) = (h_1, h_2, dots, h_m)$。则当 $vc(h) -> 0$ 时，
  $
    f(vc(a) + vc(h)) = f(vc(a)) + sum_(i = 1)^m f_(x_i)(vc(a)) h_i + 1 / 2 sum_(i = 1)^m sum_(j = 1)^m f_(x_i x_j)(vc(a)) h_i h_j + o(norm(vc(h))^2)
  $
]

#theorem[带Lagrange余项的二阶公式][
  在同样条件下，存在 $theta in (0, 1)$，使
  $
    f(vc(a) + vc(h)) = f(vc(a)) + sum_(i = 1)^m f_(x_i)(vc(a)) h_i + 1 / 2 sum_(i = 1)^m sum_(j = 1)^m f_(x_i x_j)(vc(a) + theta vc(h)) h_i h_j
  $
]

#remark[驻点附近主要看二次项][]
若 $nabla f(vc(a)) = 0$，则Taylor公式化为
$
  f(vc(a) + vc(h)) - f(vc(a)) = 1 / 2 sum_(i = 1)^m sum_(j = 1)^m f_(x_i x_j)(vc(a)) h_i h_j + o(norm(vc(h))^2)
$
因此极值判别归根到底是在研究Hessian对应二次型的符号。

#intuition[一次项决定倾斜，二次项决定弯曲][]
Taylor展开可以理解成把函数在一点附近拆成不同层级的局部信息：常数项给高度，一次项给倾斜，二次项给弯曲。极值问题里，一次项若没消失，图像立刻就会往某个方向升降；只有一次项消失后，二次项的“碗形”或“马鞍形”才开始主导局部形状。

#example[二元指数函数的二阶展开][
  对
  $
    f(x, y) = e^(x + y)
  $
  在 $(0, 0)$ 附近，有
  $
    f(x, y) = 1 + x + y + 1 / 2 (x + y)^2 + o(norm((x, y))^2)
  $
]

#connection[从局部展开到局部解方程][]
Taylor展开告诉我们：只要局部结构足够平滑，函数在一点附近就能被低次多项式精细逼近。另一个同样重要的局部问题是：当关系式不是直接写成显函数，而是由一个方程给出时，能否在局部把某个变量解出来？这就进入隐函数定理。

= 隐函数

#motivation[不是每条曲线都愿意直接写成显函数][]
前面讨论的大多是显函数，但许多几何对象天然由方程
$
  F(x_1, dots, x_n, y) = 0
$
给出，而不是直接写成 $y = phi(x_1, dots, x_n)$。隐函数定理说明：只要相应偏导不退化，局部上仍然可以把它解成普通函数，并且还能继续求导。

#theorem[单个隐函数定理][
  设 $F(x_1, dots, x_n, y)$ 在点 $(vc(a), b)$ 的某邻域内具有连续偏导数，且
  $
    F(vc(a), b) = 0, quad F_y(vc(a), b) != 0
  $
  则存在 $vc(a)$ 的某邻域 $U$ 与唯一函数
  $
    y = phi(x_1, x_2, dots, x_n)
  $
  使得 $phi(vc(a)) = b$，并对一切 $vc(x) in U$ 都有
  $
    F(vc(x), phi(vc(x))) = 0
  $
  而且 $phi$ 可微，并满足
  $
    phi_(x_i) = - frac(F_(x_i), F_y) quad (i = 1, 2, dots, n)
  $
]

#remark[条件 $F_y != 0$ 的几何意义][]
它表示在该点附近，方程 $F = 0$ 对 $y$ 的变化足够敏感，因此等值面不会与 $y$ 方向平行到无法解出 $y$。若这个偏导为 $0$，局部显式表示就可能失败。

#intuition[能不能解出一个变量，取决于它是不是真的在动][]
把方程 $F = 0$ 看成一张曲面。若沿 $y$ 方向稍微动一下，$F$ 就立刻变化，那么这张曲面在局部就不会“竖起来卡住”，于是我们能把 $y$ 唯一地写成其他变量的函数；若沿 $y$ 方向怎么动都几乎不变，就可能出现一处拐折或竖直切向，显函数表示便不稳定。

#example[球面局部可以解出上半球][
  对方程
  $
    F(x, y, z) = x^2 + y^2 + z^2 - 1 = 0
  $
  在点 $(0, 0, 1)$ 附近有
  $
    F_z(0, 0, 1) = 2 != 0
  $
  因而可在该点附近把球面写成
  $
    z = phi(x, y) = (1 - x^2 - y^2)^(1 / 2)
  $
  并且
  $
    phi_x = - x / (1 - x^2 - y^2)^(1 / 2), quad phi_y = - y / (1 - x^2 - y^2)^(1 / 2)
  $
]

#connection[从方程的局部可解到几何图像][]
一旦一个方程在局部能解成显函数，偏导数就不再只是抽象符号，而会直接表现为切平面、法向量和最速上升方向等几何对象。

= 偏导数和几何

#motivation[导数把公式变成几何][]
在多元函数里，偏导数不仅是数字；它们还描述曲面的切向变化、等值面的法向方向以及函数上升最快的方向。梯度之所以重要，就在于它同时统摄了这些几何信息。也正因为如此，几何视角会自然把我们带到极值问题。

#theorem[曲面的切平面][
  若 $z = f(x, y)$ 在点 $(a, b)$ 可微，记 $z_0 = f(a, b)$，则曲面在点 $(a, b, z_0)$ 处的切平面方程为
  $
    z - z_0 = f_x(a, b) (x - a) + f_y(a, b) (y - b)
  $
]

#theorem[梯度是等值面的法向量][
  设
  $
    S = {vc(x) in RR^m : F(vc(x)) = c}
  $
  若 $F$ 在 $vc(a) in S$ 处可微且 $nabla F(vc(a)) != 0$，则 $nabla F(vc(a))$ 垂直于 $S$ 在 $vc(a)$ 处的切空间；在三维情形下，它就是曲面 $F(x, y, z) = c$ 在该点的法向量。
]

#theorem[最速上升方向][
  若 $f$ 在 $vc(a)$ 处可微，则对任意单位向量 $vc(l)$ 都有
  $
    D_(vc(l)) f(vc(a)) = ip(nabla f(vc(a)), vc(l)) <= norm(nabla f(vc(a)))
  $
  等号成立，当且仅当 $vc(l)$ 与 $nabla f(vc(a))$ 同向。因而梯度方向是函数增长最快的方向，$-nabla f(vc(a))$ 是下降最快的方向。
]

#intuition[梯度就是等高线上的“最陡上山方向”][]
想象平面上画的是地形等高线。站在某点时，沿等高线走高度几乎不变，所以那不是上山方向；最陡上山的方向必须垂直穿过等高线，而这个方向正是梯度方向。这也是为什么梯度同时和“法向量”“最快增长”这两个几何角色绑定在一起。

#remark[图像与等值面两种视角][]
对图像 $z = f(x, y)$，偏导数给出切平面的斜率；对等值面 $F = c$，梯度给出法向量。两种说法其实是一回事，因为图像也可以写成
$
  F(x, y, z) = f(x, y) - z = 0
$

#example[球面的法向量][
  对球面
  $
    x^2 + y^2 + z^2 = R^2
  $
  取
  $
    F(x, y, z) = x^2 + y^2 + z^2
  $
  则
  $
    nabla F(x, y, z) = (2 x, 2 y, 2 z)
  $
  所以球面在点 $(x_0, y_0, z_0)$ 处的法向量正比于 $(x_0, y_0, z_0)$，也就是沿半径方向。
]

#connection[几何解释为什么能导向极值][]
切平面、法向量和梯度方向这些几何对象的价值，在于它们能把“哪里可能有极值”这件事说清楚：在内点极值处，一阶变化必须先消失，之后才轮到二阶弯曲起决定作用。

= 无条件极值

#motivation[局部极值先看一阶，再看二阶][]
几何上看，内点极值处不应存在“立刻上升”或“立刻下降”的首选方向。解析上看，这正对应一阶项先消失，然后再由二阶项主导局部形状：若一阶项不消失，函数马上会朝某个方向增减，不可能是内点极值。

#definition[局部极大值与局部极小值][
  设 $vc(a)$ 是定义域的内点。
  + 若存在 $vc(a)$ 的某邻域，使对其中一切 $vc(x)$ 都有
    $
      f(vc(x)) <= f(vc(a))
    $
    则称 $f$ 在 $vc(a)$ 处取得局部极大值。
  + 若存在某邻域，使对其中一切 $vc(x)$ 都有
    $
      f(vc(x)) >= f(vc(a))
    $
    则称 $f$ 在 $vc(a)$ 处取得局部极小值。
]

#theorem[内点极值的必要条件][
  若 $f$ 在内点 $vc(a)$ 处可微，且 $f$ 在 $vc(a)$ 处取得局部极值，则
  $
    nabla f(vc(a)) = 0
  $
]

#definition[驻点][
  满足
  $
    nabla f(vc(a)) = 0
  $
  的点称为 $f$ 的驻点。
]

#theorem[二阶全微分判别法][
  设 $f$ 在驻点 $vc(a)$ 的某邻域内具有连续二阶偏导数。
  + 若二阶全微分 $dif^2 f(vc(a))$ 对一切非零增量都大于 $0$，则 $f$ 在 $vc(a)$ 处取得局部极小值。
  + 若二阶全微分对一切非零增量都小于 $0$，则 $f$ 在 $vc(a)$ 处取得局部极大值。
  + 若二阶全微分既取正值又取负值，则 $vc(a)$ 不是极值点，而是鞍点。
]

#corollary[二元函数的判别式][
  设 $(a, b)$ 是二元函数 $f(x, y)$ 的驻点，记
  $
    A = f_(x x)(a, b), quad B = f_(x y)(a, b), quad C = f_(y y)(a, b), quad D = A C - B^2
  $
  则
  + 当 $D > 0$ 且 $A > 0$ 时，$f$ 在 $(a, b)$ 处取局部极小值。
  + 当 $D > 0$ 且 $A < 0$ 时，$f$ 在 $(a, b)$ 处取局部极大值。
  + 当 $D < 0$ 时，$(a, b)$ 是鞍点。
  + 当 $D = 0$ 时，单靠二阶判别不能下结论。
]

#remark[为什么 $D = 0$ 不够][]
当二次项退化时，更高阶项可能决定局部形状。例如 $x^4 + y^4$ 在原点有极小值，但它的二次项完全消失；所以判别式为 $0$ 时必须另作分析。

#intuition[极值判别就是分辨“碗”“倒碗”和“马鞍”][]
若驻点附近在所有小方向上都往上抬，那它像碗底，是极小值；若都往下压，它像倒扣的碗，是极大值；若有的方向往上、有的方向往下，它就像马鞍。Hessian做的事，本质上就是把这种局部弯曲形状编码成一个二次型来检查。

#example[典型的极值点与鞍点][
  + 对
    $
      f(x, y) = x^2 + y^2
    $
    原点是局部极小值。
  + 对
    $
      f(x, y) = - x^2 - y^2
    $
    原点是局部极大值。
  + 对
    $
      f(x, y) = x^2 - y^2
    $
    原点沿 $x$ 方向增大、沿 $y$ 方向减小，因此是鞍点。
]

#connection[若允许的方向被限制住][]
无条件极值默认自变量可以朝任意小方向移动。但若变量只能沿某条曲线或某个曲面滑动，那么“所有方向”就要改成“所有允许的切向方向”，这便进入条件极值。

= 条件极值

#motivation[极值被约束在曲面上][]
当自变量不能自由运动，而必须留在约束方程给出的曲线或曲面上时，极值条件就不再是 $nabla f = 0$。真正的要求是：沿一切允许的切向方向，$f$ 的一阶变化都为零。Lagrange乘数法正是把这一几何条件翻译成代数方程。

#theorem[Lagrange乘数法][
  设 $f, g$ 在点 $vc(a)$ 的某邻域内可微，且 $g(vc(a)) = 0$、$nabla g(vc(a)) != 0$。若 $f$ 在约束曲面
  $
    g(vc(x)) = 0
  $
  上于 $vc(a)$ 处取得条件极值，则存在实数 $lambda$，使
  $
    nabla f(vc(a)) = lambda nabla g(vc(a))
  $
  并且同时满足
  $
    g(vc(a)) = 0
  $
]

#theorem[多重约束的乘数法][
  若约束为
  $
    g_1(vc(x)) = 0, quad g_2(vc(x)) = 0, quad dots, quad g_r(vc(x)) = 0
  $
  并且这些约束的梯度在候选点线性无关，则条件极值点必须满足
  $
    nabla f(vc(a)) = lambda_1 nabla g_1(vc(a)) + lambda_2 nabla g_2(vc(a)) + dots + lambda_r nabla g_r(vc(a))
  $
]

#remark[乘数法给的是候选点，不是最终答案][]
解出乘数方程后，还需要回到约束条件上比较函数值，或结合二阶条件继续判别。乘数法负责找出可能发生极值的点，但不会自动替你完成筛选。

#intuition[条件极值时，最优点上两种“法向”会对齐][]
在约束曲面上做局部移动时，切向方向才是允许的方向。若某点是条件极值，那么沿所有允许切向方向的一阶变化都应为 $0$，于是 $nabla f$ 必须垂直于约束曲面的切空间；而 $nabla g$ 本来就是这个约束曲面的法向量，所以两者只能平行，这就是Lagrange乘数法背后的几何图像。

#example[单位圆上的 $x y$ 极值][
  在约束
  $
    x^2 + y^2 = 1
  $
  下求
  $
    f(x, y) = x y
  $
  的极值。令
  $
    g(x, y) = x^2 + y^2 - 1
  $
  则
  $
    nabla f = (y, x), quad nabla g = (2 x, 2 y)
  $
  方程
  $
    (y, x) = lambda (2 x, 2 y)
  $
  连同 $x^2 + y^2 = 1$ 一起求解，可得候选点
  $
    (1 / 2^(1 / 2), 1 / 2^(1 / 2)), quad (- 1 / 2^(1 / 2), - 1 / 2^(1 / 2))
  $
  $
    (1 / 2^(1 / 2), - 1 / 2^(1 / 2)), quad (- 1 / 2^(1 / 2), 1 / 2^(1 / 2))
  $
  前两点给出最大值 $1 / 2$，后两点给出最小值 $- 1 / 2$。
]

多元微分学的主线可以概括为：先用可微把局部变化线性化，再用链式法则把复杂变量关系拆开，再借助Taylor公式与梯度、Hessian把几何形状和极值问题转成代数判断。这些工具共同构成了后续多元积分与更高维分析的局部语言。
