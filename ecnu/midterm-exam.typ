#import "../../libs.typ": *
#import "../syms.typ": *

#show: ilm.with(
  title: [Mathematical Analysis\ Midterm Exam\ V2],
  date: datetime.today(),
  author: "Junfeng Lve",
  abstract: [Review notes on midterm exam of _数学分析_.],
)
#show: setup

#human[警告][
  本文档由人类编写，可能含有错误。
]

我们先来看一下这三章的目录：

#overview[Chapter 10~12][
  三章内容依次从函数项级数，过渡到Euclid空间上的极限与连续，最后进入多元函数的微分学与极值问题。
]

// format off
#table(
  columns: (1fr, 1fr),
  column-gutter: 1.2em,
  align: left + top,
  stroke: none,
  inset: 0pt,
  [
    + *第十章 函数项级数*
      - § 1 函数项级数的一致收敛
        + 点态收敛
        + 函数项级数（或函数序列）的基本问题
        + 函数项级数（或函数序列）的一致收敛性
      - § 2 一致收敛级数的判别与性质
        + 一致收敛的判别
        + 一致收敛级数的性质
        + 处处不可导的连续函数之例
      - § 3 幂级数
        + 幂级数的收敛半径
        + 幂级数的性质
      - § 4 函数的幂级数展开
        + Taylor 级数与余项公式
        + 初等函数的 Taylor 展开
      - § 5 用多项式逼近连续函数

    + *第十一章 Euclid空间上的极限和连续*
      - § 1 Euclid空间上的基本定理
        + Euclid空间上的距离与极限
        + 开集与闭集
        + Euclid空间上的基本定理
        + 紧集
      - § 2 多元连续函数
        + 多元函数
        + 多元函数的极限
        + 累次极限
        + 多元函数的连续性
        + 向量值函数
      - § 3 连续函数的性质
        + 紧集上的连续映射
        + 连通集与连通集上的连续映射
  ],
  [
    + *第十二章 多元函数的微分学*
      - § 1 偏导数与全微分
        + 偏导数
        + 方向导数
        + 全微分
        + 梯度
        + 高阶偏导数
        + 高阶微分
        + 向量值函数的导数
      - § 2 多元复合函数的求导法则
        + 链式法则
        + 一阶全微分的形式不变性
      - § 3 中值定理和 Taylor 公式
        + 中值定理
        + Taylor 公式
      - § 4 隐函数
        + 单个方程的情形
        + 多个方程的情形
        + 逆映射定理
      - § 5 偏导数在几何中的应用
        + 空间曲线的切线和法平面
        + 曲面的切平面与法线
      - § 6 无条件极值
        + 无条件极值
        + 函数的最值
        + 最小二乘法
        + “牧童”经济模型
      - § 7 条件极值问题与 Lagrange 乘数法
        + Lagrange 乘数法
        + 一个最优价格模型
  ],
)
// format on

既然有了目录，我们就可以宏观地来看一下这几章都讲了什么，需要掌握什么。

= 第十章

== 函数项级数

承接第九章，从数项级数变为函数项级数，增加了（一个）自由度。我们同样想要研究和数项级数相同的问题：在什么时候这个级数会收敛？我们如何判断这个级数的收敛？如果级数收敛，会得到什么更好的性质？

对于第一个问题，我们有三种收敛：
+ *点态收敛*：我们将每一个定义域内的 $x$ 视作常量，于是函数项级数变为数项级数。他们最终可能会收敛到一组数，称为极限函数。点态收敛是局部性质，因为仅仅和每个孤立的 $x$ 相关，不和别的 $x$ 产生关系。
+ *一致收敛*：研究函数列 ${s_n}_(n = 1)^oo$，如果 $sup_(x in D) abs((s_n - s_oo)(x)) =: r_n$ 可以被压制为无限小，则 ${s_n}$ 一致地收敛到 $s_oo$。这是基于点态收敛的，所以我们往往可以先求出 $s_oo (x)$，而后再验证我们的猜想。
+ *正常收敛*：研究级数 $sum_(n = 1)^oo u_n (x)$，其中足够大的 $u_n (x)$ 可以被压制为 $M_n$（$sup_(x in D) abs(u_n (x)) < M_n$），而且满足 $sum_(n = 1)^oo M_n$ 是收敛的正项级数，则 $sum u_n (x)$ 正常收敛。

#remark[解题时][

  如果题目中给出的是函数列 ${s_n}$ 的形式：
  + 一定是求出 $s_oo$！
  + 进行连续性分析。如果 $s_oo$ 不连续而 $s_n$ 连续，则直接逆用连续性定理，不一致收敛。
  + 求出余项 $r_n := abs(s_oo - s_n)$，分析其行为。
    - 求导找上确界（放缩也可以，尤其适用于开区间上的内闭一致收敛判断）：
      + 最值不趋于零，构造极值点点列判断不一致收敛。
      + 最值趋于零，则一致收敛。
    - 直接构造 ${x_n}_(n = 1)^oo$ 使得 $r_n (x_n) arrow.not 0$。

  如果题目中给出的是函数项级数 $sum u_n (x)$ 的形式：
  + 分析 $u_n (x)$ 是否点态收敛到 $0$。
  + 分析 $abs(u_n (x))$ 能否被收敛级数控制（Weierstrass判别法）。
    - p级数：$sum 1/n^p, quad p > 1$
    - 几何级数：$sum 1/x^n, quad x > 1$
    - Bertrand级数：$sum 1/(n ln^p n), quad p > 1$（积分判别法）
  + 尝试求出 $s_n$ 或 $s_oo$。
  + 尝试求出 $r_n := sum_(i = n)^oo u_i (x)$ 或 $r_(n, p) := sum_(i = n)^(n + p) u_i (x)$。后者常用于Cauchy收敛原理逆用，常常令 $p = n$。

  见到三角函数/单调有上界的函数首先想到Abel/Dirichlet判别法。\
  见到关于 $n$ 单调，且 $s_n$ 和 $s_oo$ 连续的首先想到Dini判别法。\
  如果题目中的函数是分段函数/定义域为开区间，则需要注意函数在区间端点上的表现。
]

对于第二个问题，主要讨论连续性，可导性和Riemann可积性。

- *连续性*：${f_n}_(n = 1)^oo$ 一致收敛到 $f_oo$。如果充分大的 $f_n$ 都是连续函数，则 $f_oo$ 也是连续函数。
- *可积性*：定义域为闭区间的函数列 ${f_n}$ 一致收敛到 $f_oo$。如果充分大的 $f_n$ 都可积，则 $f_oo$ 可积。
- *可导性*：闭区间上的函数列 ${f_n}$ 点态收敛到 $f_oo$，$f'_n$ 连续，且一致收敛到 $f'_oo$，则 $f_oo$ 可导，且 $(f_oo)' = f'_oo$。

#remark[做题时][

  只是加了包装的函数项级数判断问题。考试时灵活使用定理即可。
]

== 幂级数

有一类特殊的函数项级数——幂级数，是形式上为 $sum_(n = 1)^oo a_n (x - x_0)^n$的函数项级数。下面仅讨论 $x_0 = 0$ 的情况。

定义收敛半径
$ R := 1 / (display(overline(lim_(n -> oo))) root(n, a_n)) $
或者
$
  R := lim_(n -> oo) abs((a_n)/(a_(n + 1)))
$

则级数在 $(-R, +R)$ 上内闭一致收敛（幂级数收敛可以导出连续、可积和可导）。对于 $x = plus.minus R$ 则需要额外判断。

#remark[做题时][

  我们有一些常用幂级数的求和结果：

  $
                      sum_(n = 0)^oo x^n & = 1/(1-x), quad x in (-1, 1) \
                    sum_(n = 1)^oo x^n/n & = -ln(1 - x), quad x in (-1, 1) \
                   sum_(n = 0)^oo x^n/n! & = e^x, quad x in (-oo, +oo) \
    sum_(n = 0)^oo (-1)^n (x^(2n))/(2n!) & = cos(x), quad x in (-oo, +oo) \
      sum_(n = 0)^oo binom(alpha, n) x^n & = (1 + x)^alpha, quad x in (-oo, +oo)
  $

  首先根据题目中给出的 $sum a_n x^n$ 猜测应该是由哪些母函数生成，而后通过 $times x, div x$，求导，求不定积分等等运算配凑出题目中的形式。
]

== 幂级数逼近

中值定理：
$
  f'(x + theta Delta x) = (f(x + Delta x) - f(x))/(Delta x), quad theta in (0, 1)
$

Taylor展开：
$
  f(x) = sum_(n = 0)^N (f^((n)) (x_0))/(n!) (x - x_0)^n + r_N (x)
$

Lagrange余项：
$
  r_n (x) = (f^((n + 1)) (x_0 + theta (x - x_0)))/((n + 1)!) (x - x_0)^(n + 1), quad theta in (0, 1)
$

Peano余项：
$
  r_n (x) = o((x - x_0)^n)
$

Cauchy余项（解题中重要性不高）：
$
  r_n (x) = (f^((n + 1)) (x_0 + theta (x - x_0)))/(n!) (1 - theta)^n (x - x_0)^(n + 1), quad theta in (0, 1)
$

#remark[解题时][

  求初等函数的复合函数的有限次幂级数展开的时候多使用代入法。
]

= 第十一章

== 多元点集

关于 $n$ 为Euclid空间 $RR^n$ 上的点集 $S$，我们对 $RR^n$ 中的点有如下分类：
- 内点：存在小邻域是 $S$ 的子集。
- 外点：存在小邻域是 $RR \\ S$ 的子集。
- 边界点：任意邻域都同时包含 $S$ 和 $RR \\ S$ 的点。

#notation[关于 $RR^n$ 上的点集 $S$][

  / $delta S$: 边界。
  / $S^circle.small$: 内部。
  / $overline(S) := delta S union S^circle.small$: 传递闭包。
  / $op(d)(vc(x), S) := display(inf_(vc(y) in S)) {norm(vc(x) - vc(y))}$: 点到点集的最小距离。
  / $op(d)(S) := display(sup_(vc(x), vc(y) in S)){norm(vc(x) - vc(y))}$: 点集的直径。
]

对 $S$ 有如下分类：
- 闭集：$S$ 的所有边界点都在 $S$ 中。\
  等价定义：
  - $overline(S) = S$
  - $forall {vc(x)_n}_(n = 1)^oo subset S, vc(x)_n arrow vc(x)_0: vc(x)_0 in S$
  - $RR \\ S$ 是开集。
- 开集：$S$ 的所有点都是内点。\
  等价定义：
  - $S inter delta S = emptyset <=> delta S subset RR \\ S$
  - $forall vc(x) in S: d(vc(x), RR \\ S) > 0$
  - $RR \\ S$ 是闭集。
- 既不是开集，也不是闭集：这是大多数集合的类型。
- 紧集：对于任意开覆盖都有有限子覆盖。\
  等价定义：闭且有界。

#remark[做题时][

  $overline(S)$ 中要么是聚点要么是孤立点。

  如果考判断，多想想 $inter.big {(-1/n, +1/n)} = {0}, union.big {[-1+1/n, 1-1/n]} = (-1, 1)$
]

== 多元函数

研究多元函数的极限行为。多元函数上存在两种极限：
- *累次极限*：例如对 $f(vc(x))$ 求在 $vc(x)_0$ 处的极限，我们首先求 $f_x_1 (vc(x)) := lim_(x_1 -> vc(x)_0^1) f(vc(x))$，然后求 $f_(x_1 x_2) := lim_(x_2 -> vc(x)_0^2) f_x_1 (vc(x))$，把多元极限分为多次一元极限。\
  其几何直觉是从 $vc(x)$ 走一个 $n$ 个固定的线段组成的折线，到达最终逼近的 $vc(x)_0$
- *重极限*：例如对 $f(vc(x))$ 求在 $vc(x)_0$ 处的极限，我们同时令 $x_1 -> vc(x)_0^1, dots, x_n -> vc(x)_0^n$，但是这样有很多走法。只有在所有走法，也即是 $vc(x)_0$ 周边的小邻域中都"一致地"逼近常数时才是有极限。\
  其级和直觉是从任何一个方向，包括 $y = k x, y = x^alpha$（对于 $vc(x)_0 = (0, 0)$）等等都收敛到同一个数的时候才有效。

#caveat[重极限不是累次极限的递进][
  #align(center, image("/assets/image-108.png", width: 50%))

  经典反例
  $
    f(x, y) = cases(
      (x^2 + y^2) sin 1/x cos 1/y\, quad & x y eq.not 0,
      0\, & "otherwise"
    )
  $
  在 $(0, 0)$ 处重极限存在，但是累次极限不存在。、

  原因是因子 $(x^2 + y^2)$ 原本是能够压制后面的 $sin 1/x cos 1/y$ 的，但是在重极限中，其中一方为有限数，导致 $(x^2 + y^2)$ 不是无穷小量，进而发散。
]

#remark[做题时][

  初等函数的复合在定义域内是连续函数。可以直接带入。

  证明极限存在：
  - 使用放缩，尽量全部放缩到 $rho := norm(Delta vc(x))$ 的尺度。\
    常用不等式：
    $
      abs(x) < rho, abs(y) < rho\
      abs(x y) < rho^2/2 \
      abs(x) + abs(y) < sqrt(2) rho
    $
  - 化为极坐标函数。尤其适用于多项式分式。
  - 分离有界量，例如 $(x y)/(x^2 + y^2)$ 等等

  证明极限不存在：
  - 寻找不同路径，使得极限值不同。路径是以 $vc(x)_0$ 为聚点的隐函数。\
    如果分式多项式分母中存在自变量的不同阶，例如 $x, y^2$，可以直接令 $x = y^2$，以此轨迹逼近。
  - 构造点列 ${vc(x)_n^1}_(n = 1)^oo, {vc(x)_n^2}_(n = 1)^oo$，使得 $lim_(n -> oo) f(vc(x)_n^1) eq.not lim_(n -> oo) f(vc(x)_n^2)$。

  可以使用等价无穷小替换，但是只能使用一元等价无穷小，例如 $sin(u) tilde u, u = x + y$ 这一类。
]

== 连续多元函数的性质

+ 紧集连续映射后仍紧。
+ 紧集上连续函数有界。
+ 紧集上连续函数能取到最值。
+ 紧集上的连续函数一致连续。
+ 连通集经过连续映射后仍为连通集。
+ 紧集经过连续函数映射成闭区域。
+ 有界闭区域上的连续函数能取到最值及中间的一切值。

= 第十二章

多元微分学的主线只有一句话：用线性对象读懂局部变化。偏导和方向导数是在试探方向，全微分和梯度是在打包一阶信息，Taylor公式和Hessian则负责读出二阶弯曲。几何、隐函数和极值问题，本质上都在反复使用这些局部信息。

== 偏导、方向导数、全微分

$display(partial/(partial x_i))$ 算子要敢于用：求 $x_i$ 偏导时，其他自变量都暂时视作常数。

偏导只沿坐标轴看变化；方向导数沿任意单位方向 $vc(l)$ 看变化：
$
  D_(vc(l)) f(vc(a)) := lim_(t -> 0) (f(vc(a) + t vc(l)) - f(vc(a))) / t
$

可微则要求所有小位移都服从同一个线性主部。记 $vc(h) = (h_1, dots, h_n)$，如果
$
  f(vc(a) + vc(h)) - f(vc(a)) = L(vc(h)) + o(norm(vc(h)))
$
其中 $L$ 是线性函数，则 $f$ 在 $vc(a)$ 处可微。

若 $f$ 可微，则线性主部只能是
$
  L(vc(h)) = sum_(i = 1)^n f_(x_i)(vc(a)) h_i = ip(nabla f(vc(a)), vc(h))
$
其中
$
  nabla f(vc(a)) := (f_(x_1)(vc(a)), dots, f_(x_n)(vc(a)))
$
于是
$
  dif f = sum_(i = 1)^n f_(x_i)(vc(a)) dif x_i, quad
  D_(vc(l)) f(vc(a)) = ip(nabla f(vc(a)), vc(l))
$

#caveat[偏导存在不等于可微][
  偏导连续可以推出可微；可微可以推出连续、偏导存在，并保证方向导数的梯度公式成立。反向一般都不成立。考试中遇到分段函数，不能只算 $f_x(0, 0), f_y(0, 0)$，还要检查
  $
    (f(vc(a) + vc(h)) - f(vc(a)) - ip(nabla f(vc(a)), vc(h))) / norm(vc(h)) arrow 0
  $
]

== 链式法则

链式法则的口诀是：从根到叶，路径相乘，同类相加。若
$
  z = f(y_1, dots, y_m), quad y_j = y_j(x_1, dots, x_n)
$
则
$
  frac(partial z, partial x_i) = sum_(j = 1)^m frac(partial f, partial y_j) frac(partial y_j, partial x_i)
$

一阶全微分具有形式不变性：
$
  dif z = sum_(j = 1)^m f_(y_j) dif y_j
$
不管 $y_j$ 是自变量还是中间变量，都先按这个形式写，再把每个 $dif y_j$ 展开。

#remark[做题时][
  先画变量依赖树。每一条从底层变量到目标函数的路径都贡献一个偏导乘积，所有路径加起来就是答案。不要在脑中硬展开。

  形式不变性只对一阶全微分可靠；二阶及更高阶微分在换元后会多出额外项。
]

== 高阶微分与 Taylor 公式

一阶项看倾斜，二阶项看弯曲。若二阶偏导在点附近连续，则混合偏导可交换：
$
  f_(x_i x_j)(vc(a)) = f_(x_j x_i)(vc(a))
$

二阶全微分是Hessian对应的二次型：
$
  dif^2 f(vc(a)) = sum_(i = 1)^n sum_(j = 1)^n f_(x_i x_j)(vc(a)) dif x_i dif x_j
$

多元中值定理的本质是把 $vc(a)$ 到 $vc(a) + vc(h)$ 的线段当成一元函数。若线段位于定义域内且 $f$ 可微，则
$
  f(vc(a) + vc(h)) - f(vc(a)) = ip(nabla f(vc(a) + theta vc(h)), vc(h)), quad theta in (0, 1)
$

若 $f$ 具有连续二阶偏导，则二阶 Taylor 公式最常用：
$
  f(vc(a) + vc(h)) = f(vc(a)) + ip(nabla f(vc(a)), vc(h)) + 1 / 2 sum_(i = 1)^n sum_(j = 1)^n f_(x_i x_j)(vc(a)) h_i h_j + o(norm(vc(h))^2)
$

#remark[极值为什么看二阶][
  若 $nabla f(vc(a)) = 0$，一次项消失，局部形状就由 Hessian 二次型主导。正定像碗底，负定像山顶，不定像马鞍。
]

== 隐函数

隐函数题的本质不是把函数显式解出来，而是对方程求微分。对
$
  F(x_1, dots, x_n, y) = 0
$
若 $F$ 有连续偏导，且在目标点有 $F_y eq.not 0$，则局部可以解出 $y = phi(x_1, dots, x_n)$，并且
$
  frac(partial y, partial x_i) = - frac(F_(x_i), F_y)
$

方程组也同理：对每个方程求微分，把要求的偏导当未知量，解线性方程组。非退化条件就是相应Jacobian行列式不为 $0$。

#remark[做题时][
  不要急着解出 $y$。先写 $F = 0$，再对指定自变量求偏导；所有变量之间的依赖关系都会自动出现在链式法则里。
]

== 偏导数与几何

#align(center, [#strong([先分清对象，再选择向量])])

+ 显式曲面 $z = f(x, y)$：切平面由一阶线性化给出。
  $
    z - z_0 = f_x (x_0, y_0) (x - x_0) + f_y (x_0, y_0) (y - y_0)
  $
+ 等值曲面 $F(x, y, z) = c$：若 $nabla F(x_0, y_0, z_0) eq.not 0$，法向量是 $nabla F(x_0, y_0, z_0)$。
  $
    ip(nabla F(x_0, y_0, z_0), vec(x - x_0, y - y_0, z - z_0)) = 0
  $
+ 参数曲线 $vc(r)(t)$：切向量是 $vc(r)'(t_0)$。
  $
    vec(x, y, z) = vc(r)(t_0) + s vc(r)'(t_0)
  $
+ 两曲面交线 $F = 0, G = 0$：若两个法向量不平行，切向量可取 $nabla F times nabla G$，因为它同时垂直于两个法向量。

#caveat[切向量和法向量不要混用][
  曲线天然给切向量，曲面天然给法向量。切线用切向量，法平面也用切向量作法向量；法线用曲面法向量，切平面也用曲面法向量。
]

== 极值

无条件极值先找驻点。若 $f$ 可微且在内点 $vc(a)$ 处取得局部极值，则
$
  nabla f(vc(a)) = 0
$

二元函数的二阶判别最常用。设
$
  A = f_(x x)(a, b), quad B = f_(x y)(a, b), quad C = f_(y y)(a, b), quad D = A C - B^2
$
则
+ $D > 0, A > 0$：局部极小。
+ $D > 0, A < 0$：局部极大。
+ $D < 0$：鞍点。
+ $D = 0$：二阶判别失效，改用定义、高阶项或路径分析。

条件极值用Lagrange乘数法。若约束为
$
  g_j (vc(x)) = 0, quad j = 1, 2, dots, r
$
且约束梯度线性无关，则候选点满足
$
  nabla f(vc(x)) = sum_(j = 1)^r lambda_j nabla g_j (vc(x)), quad g_j (vc(x)) = 0
$

使用Lagrange函数的Hessian二次型判断的时候可以令 $vc(x) = mt(P) vc(y)$，其中 $mt(P)$ 的列向量是 $ker(mt(J))$ 的一组基，$mt(J)$ 是约束的Jacobian矩阵。

#remark[做题时][
  + 内部点：解 $nabla f = 0$。
  + 边界点：参数化边界，或把边界当约束用 Lagrange 乘数法。
  + 紧集最值：列出所有候选点，最后比较函数值。
  + Lagrange 乘数法只负责找候选点，不负责自动判断最大最小。
]

第十二章做题可以压成一句话：先用一阶信息找候选，再用二阶信息或函数值比较筛选；变量被约束时，把“所有方向”改成“所有允许的切向方向”。
