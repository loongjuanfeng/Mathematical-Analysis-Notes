#import "../../libs.typ": *

#show: ilm.with(
  title: [Mathematical Analysis],
  date: datetime.today(),
  author: "Junfeng Lve",
  abstract: [Study notes on Chapter 10 of _数学分析_，陈纪修.],
)
#show: setup

// #todo[
//   = 函数项级数 // 这里面放各种定义
//   = 函数项级数的收敛 // 这里面放函数项级数的几种收敛类型（和对他们的直观理解），和对不收敛行为的初步归类，以及最最基础的判别方法。
//   = 函数项级数的收敛判别 // 这里面放很多判别方法，但是关系上要讲究递进和顺承。
//   = 收敛函数项级数的性质
// ]

= 函数项级数

#motivation[从数项级数到函数项级数][]
在数项级数中，我们关心的是一串数相加之后是否有极限。若每一项都依赖于自变量 $x$，就得到函数项级数
$
  sum_(n = 1)^oo u_n(x)
$
这时问题会多出一层：不仅要问对每个固定的 $x$，这串数是否收敛，还要问当 $x$ 在整个定义域里变化时，收敛过程能否被同时控制住。

#definition[函数列与函数项级数][
  设 $E$ 为一个非空集合，$u_1(x), u_2(x), dots$ 是定义在 $E$ 上的一列函数，称它为定义在 $E$ 上的函数列。形式和
  $
    sum_(n = 1)^oo u_n(x) = u_1(x) + u_2(x) + dots
  $
  称为定义在 $E$ 上的函数项级数。
]

#definition[部分和函数与和函数][
  对每个正整数 $n$，定义
  $
    s_n(x) = sum_(k = 1)^n u_k(x), quad x in E
  $
  称 $s_n(x)$ 为该函数项级数的第 $n$ 个部分和函数。若当 $n -> oo$ 时，$s_n(x)$ 依某种意义收敛于函数 $S(x)$，则称该级数收敛于 $S(x)$，并称 $S(x)$ 为它的和函数。
]

#note[始终盯住部分和][]
函数项级数本质上研究的并不是无穷多项直接相加，而是部分和函数列
$
  s_1(x), s_2(x), dots
$
的极限行为。因此很多关于函数项级数的概念与判别法，实际上都是把函数列的收敛语言应用到部分和函数列上。

#example[几何函数项级数][
  级数
  $
    sum_(n = 0)^oo x^n
  $
  对每个满足 $abs(x) < 1$ 的 $x$ 都收敛，且和函数为
  $
    1 / (1 - x)
  $
]

= 函数项级数的收敛

#motivation[为什么会有不同的收敛概念][]
对每个固定的 $x$ 来看，函数项级数只是一个普通数项级数；但分析里真正关心的，往往是极限函数是否连续、能否积分、能否微分。这些性质不能只靠每个点各自收敛来保证，于是就需要比逐点收敛更强的概念。

#definition[逐点收敛][
  设函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  定义在集合 $E$ 上。若对每个 $x in E$，数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  都收敛，即
  $
    s_n(x) -> S(x)
  $
  则称该级数在 $E$ 上逐点收敛于 $S(x)$。
]

#definition[一致收敛][
  若存在函数 $S(x)$，使得
  $
    sup_(x in E) abs(s_n(x) - S(x)) -> 0 quad (n -> oo)
  $
  则称函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上一致收敛于 $S(x)$。
]

#definition[绝对收敛与正常收敛][
  + 若对每个 $x in E$，数项级数
    $
      sum_(n = 1)^oo abs(u_n(x))
    $
    收敛，则称原级数在 $E$ 上绝对收敛。
  + 若存在非负常数列 $M_n$，使得对一切 $x in E$ 都有
    $
      abs(u_n(x)) <= M_n
    $
    且数项级数
    $
      sum_(n = 1)^oo M_n
    $
    收敛，则称原级数在 $E$ 上正常收敛。
]

#theorem[一致收敛推出逐点收敛][
  若函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上一致收敛于 $S(x)$，则它必在 $E$ 上逐点收敛于 $S(x)$。
]

逐点收敛允许不同的点以完全不同的速度靠近极限。对某些点来说，$n = 10$ 时已经很接近；对另一些点来说，可能要到 $n = 10^6$ 才开始接近。只要每个点最终都能靠近极限，就算逐点收敛。

一致收敛则要求存在一个统一的门槛：给定误差 $epsilon > 0$，总能找到同一个 $N$，使得所有 $x in E$ 在 $n >= N$ 后都满足
$
  abs(s_n(x) - S(x)) < epsilon
$
所以一致收敛真正控制的是整张图像，而不只是每一个单独的点。

#theorem[一致收敛的Cauchy判别准则][
  函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上一致收敛，当且仅当对任意 $epsilon > 0$，存在正整数 $N$，使得当 $m > n >= N$ 时，对一切 $x in E$ 都有
  $
    abs(sum_(k = n + 1)^m u_k(x)) < epsilon
  $
]

#theorem[一致收敛的必要条件][
  若函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上一致收敛，则
  $
    u_n(x) -> 0
  $
  在 $E$ 上一致成立，即
  $
    sup_(x in E) abs(u_n(x)) -> 0
  $
]

#remark[不收敛的几种典型情形][
  + *逐点发散*：存在某个 $x_0 in E$，对该点处的数项级数 $sum u_n(x_0)$，无论取多大的 $N$，部分和 $s_N(x_0)$ 都不收敛到任何有限值。
  + *逐点收敛但不一致收敛*：对每个固定的 $x$，给定 $epsilon > 0$ 都能找到 $N(x, epsilon)$ 使余项足够小，但这个 $N$ 对不同的 $x$ 可以任意大——无法找到一个对全体 $x in E$ 同时有效的 $N$。典型情形是部分和函数列存在一列极值点，这些极值点随 $n$ 趋向定义域的某个聚点，极值始终不趋于零，导致 $sup_(x in E) abs(s_n(x) - S(x)) arrow.not 0$。
  + *绝对收敛但不一致收敛*：对每个固定的 $x$，$sum abs(u_n(x))$ 收敛，但控制它所需的尾项上界随 $x$ 变化而无界，无法找到与 $x$ 无关的控制列 $M_n$。绝对收敛只是对每个 $x$ 单独成立的逐点条件，不携带任何关于 $x$ 方向的一致性信息。
  + *一致收敛但不正常收敛*：$sup_(x in E) abs(s_n(x) - S(x)) -> 0$，但 $sup_(x in E) abs(u_n(x))$ 的级数发散，即不存在满足 Weierstrass 条件的控制列。一致收敛控制的是部分和整体与极限的距离，正常收敛则要求逐项大小本身可被控制，后者更强。
]

#example[$x^n$ 在 $[0, 1]$ 上的极限][
  函数列 $x^n$ 在区间 $[0, 1]$ 上逐点收敛于这样一个函数：当 $0 <= x < 1$ 时，极限为 $0$；当 $x = 1$ 时，极限为 $1$。
  但它不是一致收敛的。因为每个 $x < 1$ 时 $x^n -> 0$，而在靠近 $1$ 的地方，$x^n$ 下降得越来越慢，始终找不到一个对全区间都有效的统一门槛。
]

#example[几何级数在不同区间上的表现][
  级数
  $
    sum_(n = 0)^oo x^n
  $
  在 $(-1, 1)$ 上逐点收敛于
  $
    1 / (1 - x)
  $
  但在整个 $(-1, 1)$ 上不是一致收敛的，因为当 $x$ 越靠近 $1$，余项就越难同时压小。若把定义域缩小到 $[-r, r]$，其中 $0 < r < 1$，则它是一致收敛的。
]

= 函数项级数的收敛判别

#strategy[判别的一条主线][]
判断函数项级数时，最先要分清目标：若只问某个点处的值，可退化为数项级数；若要讨论极限函数的连续性、积分与微分，就应优先追求一致收敛。

一个实用的顺序是：

+ 先看是否能直接验证一致Cauchy条件；
+ 若每一项都能被一个与 $x$ 无关的数项控制，优先用 Weierstrass判别法；
+ 若级数可写成振荡因子乘上衰减因子的形式，再考虑 Dirichlet判别法或 Abel判别法；
+ 若在紧区间上遇到单调逼近连续极限的部分和函数列，可考虑 Dini定理。

#theorem[Weierstrass判别法][
  若存在非负常数列 $M_n$，满足对一切 $x in E$ 都有
  $
    abs(u_n(x)) <= M_n
  $
  且数项级数
  $
    sum_(n = 1)^oo M_n
  $
  收敛，则函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上正常收敛，从而绝对且一致收敛。
]

#remark[Weierstrass判别法的实质][]
它把函数项级数的困难压缩成一个普通数项级数问题。只要每一项的大小都能被同一个与 $x$ 无关的上界控制住，那么整串函数在整个定义域上的波动就会被同步压住。

#theorem[由上确界级数判别正常收敛][
  若数项级数
  $
    sum_(n = 1)^oo sup_(x in E) abs(u_n(x))
  $
  收敛，则函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上正常收敛。
]

#example[几何级数在闭区间上的一致收敛][
  在区间 $[-r, r]$ 上，其中 $0 < r < 1$，有
  $
    abs(x^n) <= r^n
  $
  而数项级数 $sum_(n = 0)^oo r^n$ 收敛，所以
  $
    sum_(n = 0)^oo x^n
  $
  在 $[-r, r]$ 上一致收敛。
]

#theorem[Dirichlet一致判别法][
  设函数列 $a_n(x), b_n(x)$ 定义在 $E$ 上，并满足：
  + 部分和函数
    $
      A_n(x) = sum_(k = 1)^n a_k(x)
    $
    在 $E$ 上一致有界；
  + 对每个固定的 $x in E$，数列 $b_n(x)$ 关于 $n$ 单调；
  + $b_n(x) -> 0$ 在 $E$ 上一致成立。

  则函数项级数
  $
    sum_(n = 1)^oo a_n(x) b_n(x)
  $
  在 $E$ 上一致收敛。
]

#theorem[Abel一致判别法][
  设函数项级数
  $
    sum_(n = 1)^oo a_n(x)
  $
  在 $E$ 上一致收敛；又设对每个固定的 $x in E$，数列 $b_n(x)$ 关于 $n$ 单调，且存在常数 $M > 0$，使得对一切 $n$ 与一切 $x in E$ 都有
  $
    abs(b_n(x)) <= M
  $
  则函数项级数
  $
    sum_(n = 1)^oo a_n(x) b_n(x)
  $
  在 $E$ 上一致收敛。
]

Weierstrass判别法适合处理每一项都能直接压住大小的情形；Dirichlet与Abel则处理更细的结构。前者依赖有界振荡与趋零因子的配合，后者依赖已知一致收敛的级数再乘上一个温和权重后仍然稳定。

它们的共同点是：不再逐项孤立地估计，而是利用部分和的整体结构。只要这种整体结构可控，就有可能得到一致收敛。

#theorem[Dini定理在函数项级数中的应用][
  设每个 $u_n(x)$ 都在闭区间 $[a, b]$ 上连续，且部分和函数
  $
    s_n(x) = sum_(k = 1)^n u_k(x)
  $
  对每个 $x in [a, b]$ 都关于 $n$ 单调。若 $s_n(x)$ 逐点收敛于一个连续函数 $S(x)$，则该函数项级数在 $[a, b]$ 上一致收敛于 $S(x)$。
]

= 收敛函数项级数的性质

#motivation[为什么分析里更偏爱一致收敛][]
一致收敛的价值不在于它比逐点收敛更强，而在于它能把许多单个函数的良好性质传递给极限函数。连续性、可积性、可微性之所以能和无穷求和交换，往往正是因为背后有一致收敛在兜底。

#theorem[连续函数项级数的一致极限仍连续][
  若每个 $u_n(x)$ 都在集合 $E$ 上连续，且函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $E$ 上一致收敛于 $S(x)$，则 $S(x)$ 也在 $E$ 上连续。
]

#example[为什么逐点收敛不够][
  函数列 $x^n$ 在 $[0, 1]$ 上的每一项都连续，但其逐点极限在 $[0, 1)$ 上恒为 $0$，在 $x = 1$ 处等于 $1$，因此不是连续函数。这说明仅有逐点收敛时，连续性可能丢失。
]

#theorem[逐项积分][
  设每个 $u_n(x)$ 都在闭区间 $[a, b]$ 上连续，且函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $[a, b]$ 上一致收敛于 $S(x)$，则
  $
    integral_a^b S(x) dif x = sum_(n = 1)^oo integral_a^b u_n(x) dif x
  $
]

#remark[逐项积分的理解][]
一致收敛保证余项在整个区间上都能同时变小，因此其积分也会同时变小，于是先求和再积分与先积分再求和得到同一个结果。

#theorem[逐项微分][
  设每个 $u_n(x)$ 都在闭区间 $[a, b]$ 上可微，且存在点 $x_0 in [a, b]$，使数项级数
  $
    sum_(n = 1)^oo u_n(x_0)
  $
  收敛。若导函数级数
  $
    sum_(n = 1)^oo u_n'(x)
  $
  在 $[a, b]$ 上一致收敛，则原函数项级数
  $
    sum_(n = 1)^oo u_n(x)
  $
  在 $[a, b]$ 上一致收敛于某个函数 $S(x)$，且
  $
    S'(x) = sum_(n = 1)^oo u_n'(x)
  $
]

#note[逐项微分比逐项积分更严格][]
想把导数与无穷求和交换，不能只看原级数是否一致收敛，更关键的是导函数级数能否一致收敛。它反映的是局部变化率能否被整体控制，而这比控制函数值本身更强。

#theorem[正常收敛带来的整体稳定性][
  若函数项级数在 $E$ 上正常收敛，则它必在 $E$ 上绝对且一致收敛。因而在满足相应附加条件时，可以进一步得到和函数的连续性与逐项积分等性质。
]

函数项级数的核心并不是把数项级数的结论逐字搬过来，而是学会区分两种尺度：一点一点地看，还是在整个定义域上同时看。逐点收敛解决的是是否有和函数，一致收敛解决的则是这个和函数能否保留分析中的好性质。
